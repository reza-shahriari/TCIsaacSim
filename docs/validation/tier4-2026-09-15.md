# Tier 4 acceptance -- 2026-09-15

Generated by `scripts/validation_report.py`. Do not edit by hand.

    python scripts/validation_report.py --synthetic outputs/matched \
        --real-from-set halmstad_drone_detection --real-clips 6 --real-frames-per-clip 60

**This run fails, and that is the result.** Three of the five DN8 targets are exceeded, and the
discriminator separates the two sets at an AUC of 0.91 -- 38 null standard errors from chance. The
sets are trivially distinguishable.

**The dominant term is the signal path, not the radiometry, and the report says which.** The
discriminator's heaviest feature is `noise_scale`, and ME.5 measured exactly why: **306 of the 365
published clips have a robust noise scale at or below one code** -- the encoder removed the
sensor's noise entirely -- while the rendered clips retain 2-3 codes through the same nominal
codec. A CRF sweep from 18 to 36 on the rendered clips moves that from 3.1 to 2.1 and no further,
so the published recorder did something the round trip here does not reproduce: the clips run at
~128 kb/s for 320x256x30, and whatever produced that flattened the noise before or during the
encode.

So the number to act on is not the simulator's radiometry. It is M12.1's recorder model
(`agc: linear` standing in for an unverified Y16 -> 8-bit conversion) and its codec settings, both
marked ESTIMATED in their own files, and both the first suspects the attribution table below names.

**Two of the five checks could not be run at all**, for the reason ME.5 recorded: the reference
set's annotation boxes are MATLAB Video Labeler `groundTruth` MCOS objects with no Python reader,
so the contrast-ratio and edge-spread comparisons have no boxes. And §15's radiometric bias target
is reported as untestable in every run, because there is nothing radiometric to compare against.

**No README row is promoted on this run.** ADR 0068's targets are the gate, and they were not met.

| check | measured | target | verdict | note |
|---|---|---|---|---|
| histogram EMD (DN8 codes) | 24.69 | 8 | **FAIL** | L1 between the cumulative distributions; reads as codes of shift |
| PSD shape ratio | 5.698 | 1.5 | **FAIL** | radial, normalised, every bin compared |
| contrast ratio agreement | -- | 0.25 | untestable | no annotation boxes supplied; on the reference set the labels are MATLAB MCOS objects with no Python reader (ME.5), so this is untestable there |
| ESF width agreement | -- | 0.2 | untestable | no edge profiles supplied |
| discriminator AUC | 0.9066 | 0.7 | **FAIL** | 1440 vs 1440 patches, 5-fold cross-validated; +37.79 null sigma from chance (sigma = 0.011); heaviest features: noise_scale 3.98, skew 2.49, gradient_median 2.41 |
| apparent-temperature bias (K) | -- | 2 | untestable | §15 asks for 2 K against a measurement. The public data is 8-bit, post-AGC and lossy-coded, so there is no radiometric measurement to be within 2 K of. Open until a radiometric capture exists (ADR 0003, ADR 0068) |

## Where each failure points first

- **histogram EMD (DN8 codes)** -- M12.1's recorder model first (`agc: linear` standing in for an unverified Y16 -> 8-bit conversion), then the scene priors, which ME.5 could not measure at all. The radiometry is the last suspect: an unknown display mapping can move a histogram by tens of codes without any physics changing.
- **PSD shape ratio** -- M12.1's codec settings and MS.3's cloud model. ME.2b measured x264 at CRF 18 removing 95 % of a clip's temporal noise, so a mismatch here is an encoder mismatch until the encoder is matched; after that it is the spatial structure of the modelled sky.
- **discriminator AUC** -- read the heaviest features in the note. `noise_scale` or `spectrum_degenerate` at the top means the *signal path* separates the sets -- M12.1's recorder and codec -- not the physics. A physics gap shows up as `psd_slope`, `gradient_*` or `lag1_autocorrelation`.

## Not testable on this input

- **contrast ratio agreement** -- no annotation boxes supplied; on the reference set the labels are MATLAB MCOS objects with no Python reader (ME.5), so this is untestable there
- **ESF width agreement** -- no edge profiles supplied
- **apparent-temperature bias (K)** -- §15 asks for 2 K against a measurement. The public data is 8-bit, post-AGC and lossy-coded, so there is no radiometric measurement to be within 2 K of. Open until a radiometric capture exists (ADR 0003, ADR 0068)
